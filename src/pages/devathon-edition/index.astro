---
import Layout from "../../layouts/Layout.astro";
import "./devathon-edition.css";
import TeamExpandableCard from "../../components/astro/TeamExpandableCard/index.astro";
import type { Data } from "../../interfaces/DevathonTeamsData";
import { devathonTeamsData } from "../../data/devathon-teams";

const { data } = devathonTeamsData;
---

<Layout title="4ta edición">
  <main>
    <section>
      <h1>Devathon: 4ta edición</h1>
      <div class="teams-container">
        {
          data.map((teamData: Data) => {
            const { name, score } = teamData;
            return <TeamExpandableCard name={name} score={score} />;
          })
        }
      </div>
    </section>
    <section>
      <h1>Sponsors de esta edición</h1>
    </section>
    <section>
      <h1>Información general</h1>
    </section>
  </main>
</Layout>
