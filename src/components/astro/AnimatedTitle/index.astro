---
import "./styles.css";
export interface Props {
  text: string;
  color: string;
}

const { text, color } = Astro.props;
---

<h1
  class={color === "blue"
    ? "animatedTitleBlue"
    : "animatedTitleOrange"}
>
  {text}
</h1>
